<%= form_for(@note_post) do |f| %>
  <% if @note_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note_post.errors.count, "error") %> prohibited this note_post from being saved:</h2>

      <ul>
      <% @note_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
